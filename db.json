{
  "posts":  [
    { "id": 213123123,  
      "tools": [
    {
      "name": "search",
      "description": "搜索互联网上的信息",
      "parameters": {
        "query": {
          "type": "string",
          "description": "搜索关键词"
        },
        "limit": {
          "type": "number",
          "description": "返回结果数量限制"
        }
      },
      "required": ["query"]
    }
  ]},
  { "id": 213123124,  
      "tools": [
    {
      "name": "db_query",
      "description": "查询数据库信息",
      "parameters": {
        "query": {
          "type": "string",
          "description": "搜索关键词"
        },
        "limit": {
          "type": "number",
          "description": "返回结果数量限制"
        }
      },
      "required": ["query"]
    },
    {
       "name": "txn_query",
      "description": "查询交易信息",
      "parameters": {
        "txnDate": {
          "type": "string",
          "description": "交易日期"
        },
        "txnType": {
          "type": "string",
          "description": "交易类型"
        }
      },
      "required": ["txnDate","txnType"]
    }
  ]}
  ]
  
 ,
  "comments": [
    { "id": 1, "body": "some comment", "postId": 1 },
    { "id": 2, "body": "some comment", "postId": 1 }
  ],
  "profile": {
    "name": "typicode"
  }
}
